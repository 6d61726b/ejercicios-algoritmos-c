// Ejemplo cadena de caracteres (mi main)

#include <stdio.h>
#include "cadena.h"

void ejemploCadena(void);

void ejemploCadena()
{
/*
	char nombre[15];
	char n[] = "\0 juan";
	char j[] = " hola";

	// Asigno la cadena "Marcos" a nombre
	copiarCadena(nombre, "Marcos");

	// Mostramos el contenido de nombre
	printf("Nombre = %s\n", nombre);

	// Mostramos la longitud de la cadena literal "Marcos"
	printf("Longitud de %s = %d\n", "Marcos", longitud("Marcos"));

	// Mostramos la longitud de la cadena contenida en nombre
	printf("Longitud de %s = %d\n", nombre, longitud(nombre));

	printf("esta: %d\n", esVacia(nombre));
	printf("esta: %d\n", esVacia(n));
	printf("esta: %d\n", esVacia(j));

	char a[] = "Pablo";
	char b[] = "Juan";

	printf("%s vs %s = %d", a, b, compararCadena(a, b));


	char s[] = "12345";

	// asigno a n el valor numerico de s
    int n = cadenaAEntero(s);

	printf("Cadena = [%s]\n", s);
	printf("Numero = [%d]\n", n);
*/
}
